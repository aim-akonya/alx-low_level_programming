#include "main.h"

/**
* _strlen - print the size of a string
* @s: string input
* Return: length of input string
*/
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
		continue;
	}
	return (count);
}
