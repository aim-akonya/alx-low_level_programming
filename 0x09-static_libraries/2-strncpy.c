#include "main.h"

/**
* _strncpy - copy characters from one string to the pther
* @dest: destination
* @src: source
* @n: length of the string to be copied
* Return: new character array
*/
char *_strncpy(char *dest, char *src, int n)
{
	return (dest);
}
