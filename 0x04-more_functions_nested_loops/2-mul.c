#include "main.h"

/**
* mul -  multiply two numbers
* @a: an interger values representing the first number
* @b: an integer value rep the second number
* Return: - return an int value representing the result
*/
int mul(int a, int b)
{
	return (a*b);
}
